# GitHub product-program reference workers

One digest-pinned tool image exposes separate deterministic BRD-author, planner,
and feature-implementation commands. Provider app identities select one trusted
command; issue and BRD text remain inert data. The reference implementation
builds the disposable one-container status-board demo without model authority.
