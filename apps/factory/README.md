# apps/factory

Durable coordinator/worker fan-out app, ported from `barista-cloud`'s
`demos/factory` to run only through the Host API/SDK. See
`openspec/changes/apps-001-portable-agent-apps/specs/factory-app/spec.md`.
